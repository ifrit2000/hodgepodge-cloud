package com.github.cd871127.hodgepodge.cloud.lib.crypto;

public class Rsa implements Encript {
    @Override
    public byte[] encode(byte[] bytes) {
        return new byte[0];
    }

    @Override
    public byte[] dncode(byte[] bytes) {
        return new byte[0];
    }
}
