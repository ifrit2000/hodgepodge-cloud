logging:
#  level:
#    com.github.cd871127.hodgepodge.cloud: debug
  config: classpath:config/logback-spring.xml
  path: /home/anthony


spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: ${mysql.password}
    username: ${mysql.username}
    url: jdbc:mysql://${mysql.host}:${mysql.port}/${mysql.database}?useUnicode=true&characterEncoding=utf8&autoReconnect=true&useSSL=false&zeroDateTimeBehavior=CONVERT_TO_NULL&serverTimezone=UTC&allowPublicKeyRetrieval=true
